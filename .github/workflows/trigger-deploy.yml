name: Deploy to Environments

on:
  push:
    branches:
      - main
      - dev
      - 'release/*'

jobs:
  deploy:
  runs-on: labs-runner
  steps:
    uses: ./.github/workflows/deploy-enviromment.yml
    with:
      env: ${{ github.ref.name }}
    